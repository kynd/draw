import { setupDrawingTool } from '../../lib/demo/drawingTool/index.js';
import { WetStrokesDemoConfig } from '../../lib/demo/toolConfigs.js';

setupDrawingTool(new WetStrokesDemoConfig(), { square: true });
